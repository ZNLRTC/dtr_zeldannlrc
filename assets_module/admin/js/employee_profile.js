$(function () {
    var Script = {};
    (function (app) {
  
        app.init = function() {
          app.bindings();
        }
  
        app.bindings = function() {
            
            
        }

        app.init();
    })(Script);
  });